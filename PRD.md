# PRD — ProjectsLister (projls)

## 1. نظرة عامة

**ProjectsLister** برنامج طرفية تفاعلي (TUI) يتيح للمستخدم حفظ قائمة بمجلدات مشاريعه المستخدَمة بكثرة، والتنقّل بينها بسرعة من الطرفية بدلًا من كتابة مسارات طويلة يدويًا أو تذكّرها.

يُستدعى البرنامج عبر أمر واحد في الطرفية: `projls`. عند اختيار مشروع من القائمة، يُغلَق البرنامج وتنتقل الطرفية الحالية تلقائيًا إلى مجلد ذلك المشروع.

- **واجهة المستخدم:** بالكامل باللغة الإنجليزية.
- **الجمهور:** المستخدم نفسه (مشروع شخصي)، مع احتمال نشره لاحقًا كحزمة قابلة للتثبيت.

---

## 2. الأهداف

1. الوصول لأي مشروع محفوظ بضغطة أو ضغطتين بدل كتابة `cd /path/to/project`.
2. ترتيب المشاريع تلقائيًا حسب الأحدث استخدامًا (MRU – Most Recently Used).
3. تجربة استخدام بسيطة بالكامل عبر لوحة المفاتيح، بدون ماوس.
4. عمل موثوق على النظامين الرئيسيين: Windows وLinux/Ubuntu.

---

## 3. المنصات المستهدفة

| النظام | الدعم |
|---|---|
| Windows | ✅ عبر دالة PowerShell |
| Linux/Ubuntu | ✅ عبر دالة bash |

---

## 4. المكدس التقني (Tech Stack)

- **اللغة:** Python 3.10+
- **مكتبة الواجهة:** [Textual](https://textual.textualize.io/)
- **تحديد مسار الإعدادات حسب النظام:** مكتبة `platformdirs`
- **التخزين:** ملف `projects.json`
- **التوزيع:** حزمة قابلة للتثبيت عبر `pip` (أو `pipx`)، مع نقطة دخول (entry point) باسم تنفيذي، مثلًا `projectslister` أو `projls-core` (انظر القسم 8 لتوضيح سبب الفصل بين اسم البرنامج الفعلي واسم أمر الطرفية).

---

## 5. نموذج البيانات (Data Model)

كل مشروع في القائمة يتكوّن من:

| الحقل | النوع | الوصف |
|---|---|---|
| `name` | نص | اسم المشروع كما يظهر في القائمة |
| `path` | نص | المسار الكامل لمجلد المشروع |
| `last_opened` | تاريخ/وقت (ISO 8601) أو `null` | يُحدَّث **فقط** عند فتح المشروع من داخل البرنامج (وليس عند أي تعديل داخل مجلد المشروع نفسه). المشاريع التي لم تُفتَح بعد تحمل قيمة `null` وتُعرض في نهاية القائمة أو مرتبة حسب تاريخ الإضافة. |

**مثال على بنية `projects.json`:**

```json
[
  {
    "name": "مسابقة الأشاوس",
    "path": "/home/abo/projects/al-ashawes",
    "last_opened": "2026-07-28T21:14:00"
  },
  {
    "name": "مراقي المعرفة",
    "path": "/home/abo/projects/maraqi",
    "last_opened": null
  }
]
```

### مكان حفظ الملف

يُحفظ الملف في مجلد إعدادات المستخدم القياسي لكل نظام (وليس بجانب البرنامج، لأن الحزمة بعد التثبيت عبر pip تُنسخ إلى مجلد نظام لا يجوز الاعتماد عليه لتخزين بيانات المستخدم):

- **Linux/Ubuntu:** `~/.config/projectslister/projects.json`
- **Windows:** `%APPDATA%\projectslister\projects.json`

يُحدَّد هذا المسار تلقائيًا عبر `platformdirs.user_config_dir("projectslister")`، ويُنشأ المجلد تلقائيًا عند أول تشغيل إن لم يكن موجودًا.

---

## 6. الميزات الوظيفية

### 6.1 عرض القائمة الرئيسية
- تُعرض المشاريع مرتبة تلقائيًا حسب `last_opened` تنازليًا (الأحدث استخدامًا في الأعلى).
- كل سطر يعرض: الاسم، والمسار (مختصرًا إن كان طويلًا).
- شريط اختصارات ثابت أسفل الشاشة (انظر القسم 7).

### 6.2 إضافة مشروع (Add) — مفتاح `a`
- يفتح نموذج إدخال بحقلين: **Name** و **Path**.
- التحقق من الصحة (Validation):
  - الاسم غير فارغ.
  - المسار موجود فعليًا على القرص ويشير إلى مجلد.
  - رسالة خطأ واضحة داخل النموذج عند فشل التحقق، بدون إغلاق النموذج.
- عند الحفظ: يُضاف المشروع بقيمة `last_opened = null`.

### 6.3 تعديل مشروع (Edit) — مفتاح `e`
- يفتح نفس نموذج الإضافة معبّأً بالقيم الحالية للمشروع المحدَّد.
- يسمح بتعديل الاسم و/أو المسار.
- نفس قواعد التحقق من الصحة في 6.2.
- لا يُغيَّر `last_opened` عند التعديل.

### 6.4 حذف مشروع (Delete) — مفتاح `d`
- يعرض **رسالة تأكيد** قبل الحذف الفعلي (مثال: "Delete 'اسم المشروع'? [y/n]").
- عند التأكيد: يُحذَف المشروع من القائمة ومن الملف.
- عند الإلغاء: يعود للقائمة بدون أي تغيير.

### 6.5 البحث (Search) — مفتاح `/`
- يفتح شريط بحث نصي يُصفّي القائمة مباشرة (فلترة حيّة أثناء الكتابة) بناءً على اسم المشروع (وربما المسار أيضًا).
- `Esc` يُغلق البحث ويُعيد القائمة الكاملة.

### 6.6 فتح مشروع (Open) — مفتاح `Enter`
- يعرض **رسالة تأكيد** قبل الفتح (مثال: "Open 'اسم المشروع'? [y/n]").
- عند التأكيد:
  1. تحديث `last_opened` للمشروع بالتاريخ والوقت الحاليين، وحفظ الملف.
  2. طباعة المسار الكامل للمشروع على المخرج القياسي (stdout) — **هذا هو الشرط الأساسي الذي تعتمد عليه دالة `projls`** (انظر القسم 8).
  3. إغلاق البرنامج (خروج بالحالة 0).
- عند الإلغاء: يعود للقائمة.
- **ملاحظة تقنية مهمة:** لأن واجهة Textual تُشغَّل بكامل الشاشة (full-screen)، يجب طباعة المسار **بعد** خروج تطبيق Textual فعليًا (بعد `app.run()`)، وليس أثناء تشغيله، وإلا لن يظهر في المخرج القياسي بشكل صحيح.

### 6.7 الخروج بدون فتح (Quit) — مفتاح `q`
- يغلق البرنامج مباشرة بدون طباعة أي مسار (بحيث لا تنتقل الطرفية إلى أي مجلد).

---

## 7. شريط الاختصارات (Footer)

يظهر بشكل دائم أسفل الشاشة:

```
a add · e edit · d delete · / search · enter open · esc back · q quit
```

| المفتاح | الوظيفة |
|---|---|
| `a` | إضافة مشروع جديد |
| `e` | تعديل المشروع المحدَّد |
| `d` | حذف المشروع المحدَّد (بتأكيد) |
| `/` | بدء البحث |
| `Enter` | فتح المشروع المحدَّد (بتأكيد) |
| `Esc` | رجوع (إغلاق نموذج/بحث نشط) |
| `q` | خروج من البرنامج بدون فتح مشروع |

---

## 8. ربط أمر `projls` بالطرفية (Shell Integration)

نظرًا لأن أي عملية فرعية لا تستطيع تغيير مجلد عملية الطرفية الأم مباشرة، يعتمد الحل على:

1. **البرنامج الفعلي** (نقطة الدخول الأساسية، مثلًا `projectslister`) لا ينفّذ `cd` بنفسه، بل يطبع المسار المختار على stdout عند الفتح فقط (القسم 6.6).
2. **دالة shell خفيفة باسم `projls`** تُشغّل البرنامج الفعلي، وتلتقط خرجه، ثم تنفّذ `cd` به في الطرفية الحالية.

### 8.1 محتوى الدالة

**على Linux/Ubuntu (bash):**

```bash
projls() {
    local target
    target=$(projectslister)
    if [ -n "$target" ]; then
        cd "$target" || return
    fi
}
```

**على Windows (PowerShell):**

```powershell
function projls {
    $target = projectslister
    if ($target) {
        Set-Location $target
    }
}
```

### 8.2 آلية الإضافة: تلقائية وليست يدوية

**لن يُطلب من المستخدم فتح ملف `~/.bashrc` أو ملف الـ PowerShell Profile وتعديله يدويًا.** بدل ذلك، يقوم البرنامج نفسه بهذه الخطوة تلقائيًا ضمن مرحلة التغليف (انظر القسم 13، المرحلة الثانية)، عبر أمر تثبيت مخصّص (مثلًا `projectslister --install-shell` يُشغَّل مرة واحدة بعد `pip install`، أو يُنفَّذ تلقائيًا كخطوة post-install):

1. تحديد نظام التشغيل الحالي ونوع الطرفية المستخدمة (bash على Linux/Ubuntu، PowerShell على Windows).
2. تحديد المسار الصحيح لملف التهيئة المناسب (`~/.bashrc` على Linux/Ubuntu، ملف الـ Profile الخاص بـ PowerShell على Windows — يُحدَّد عبر `$PROFILE`).
3. التحقّق أولًا هل الدالة مضافة مسبقًا (لتفادي التكرار عند إعادة التشغيل).
4. إن لم تكن موجودة: إلحاق كتلة الدالة المناسبة تلقائيًا في نهاية الملف، مع تعليق واضح يوضّح أنها أُضيفت بواسطة ProjectsLister (لتسهيل التمييز أو الحذف لاحقًا).
5. إعلام المستخدم في نهاية التثبيت بأنه يحتاج فتح طرفية جديدة (أو تنفيذ `source ~/.bashrc`) لتفعيل الأمر.

### 8.3 ملاحظات
- عند الضغط على `q` (خروج بدون فتح)، لا يُطبَع شيء على stdout، فتبقى الدالة بدون أي `cd`، والطرفية لا تتغيّر.
- تعديل ملفات التهيئة عملية حساسة؛ يجب أن تكون الكتابة آمنة (نسخة احتياطية بسيطة أو تحقّق قبل الكتابة) لتفادي إتلاف الملف الأصلي للمستخدم.

---

## 9. تدفّق الاستخدام (User Flow)

```
$ projls
┌─────────────────────────────────────────────┐
│  ProjectsLister                              │
│  ┌─────────────────────────────────────────┐ │
│  │ > مسابقة الأشاوس   ~/projects/al-ashawes │ │
│  │   مراقي المعرفة    ~/projects/maraqi     │ │
│  └─────────────────────────────────────────┘ │
│  a add · e edit · d delete · / search ·      │
│  enter open · esc back · q quit              │
└─────────────────────────────────────────────┘
        │ Enter
        ▼
   Open 'مسابقة الأشاوس'? [y/n]
        │ y
        ▼
   (يُحدَّث last_opened، يُطبع المسار، يُغلق البرنامج)
        │
        ▼
$ cd ~/projects/al-ashawes   ← تنفّذها دالة projls تلقائيًا
```

---

## 10. متطلبات غير وظيفية (Non-Functional Requirements)

- **الأداء:** فتح البرنامج وعرض القائمة يجب أن يكون فوريًا (أقل من نصف ثانية) حتى مع عشرات المشاريع.
- **الاعتمادية:** يجب ألا يؤدي أي خطأ (مسار غير موجود، ملف JSON تالف) إلى تعطّل البرنامج بالكامل؛ بل عرض رسالة خطأ واضحة داخل الواجهة.
- **حماية البيانات:** كتابة ملف `projects.json` يجب أن تكون آمنة (كتابة إلى ملف مؤقت ثم استبدال الملف الأصلي) لتفادي فقدان البيانات عند إغلاق غير متوقع.
- **قابلية التوسّع مستقبلًا:** بنية الكود يجب أن تفصل منطق التخزين (data layer) عن واجهة Textual (UI layer) بشكل واضح.

---

## 11. خارج نطاق النسخة الأولى (Out of Scope)

- الترتيب اليدوي المخصّص للمشاريع (Custom manual ordering).
- التصنيفات/الوسوم (Tags/Categories) للمشاريع.
- إزالة تلقائية للدالة من ملفات التهيئة عند إلغاء التثبيت (uninstall) — يبقى الحذف يدويًا في النسخة الأولى.
- دعم macOS (غير مطلوب حاليًا).

---

## 12. معايير القبول (Acceptance Criteria)

- [ ] تشغيل `projls` من أي مكان في الطرفية (بعد التثبيت وربط الدالة) يفتح واجهة Textual.
- [ ] إضافة مشروع بمسار غير صحيح تُظهر خطأ ولا تُحفظ.
- [ ] فتح مشروع (بعد التأكيد) يُحدّث `last_opened`، وينقل الطرفية فعليًا لمجلد المشروع، ويُغلق البرنامج.
- [ ] حذف مشروع (بعد التأكيد) يزيله من القائمة والملف نهائيًا.
- [ ] البحث يُصفّي القائمة حيًّا أثناء الكتابة.
- [ ] الخروج بـ `q` لا يُغيّر مجلد الطرفية إطلاقًا.
- [ ] القائمة مرتبة دائمًا حسب `last_opened` تنازليًا.
- [ ] يعمل البرنامج بنفس السلوك على Windows وLinux/Ubuntu.

---

## 13. مراحل البناء (Build Phases)

يُبنى المشروع على مرحلتين منفصلتين، ولا تبدأ الثانية إلا بعد التأكد من نجاح الأولى:

### المرحلة 1 — بناء الكود الأساسي (Core Build)

الهدف: بناء منطق البرنامج وواجهته داخل مجلد المشروع فقط، وتجربته محليًا (تشغيله عبر `python main.py` مثلًا)، دون التفكير بعد في التثبيت أو الأمر `projls`.

يشمل ذلك:
- بنية المشروع وفصل data layer عن UI layer (القسم 10).
- تنفيذ نموذج البيانات والتخزين في `projects.json` (القسم 5)، باستخدام `platformdirs` لتحديد مسار الحفظ.
- بناء واجهة Textual: القائمة الرئيسية، شريط الاختصارات، نماذج الإضافة/التعديل، رسائل تأكيد الفتح والحذف، البحث الحي (القسم 6، 7).
- التحقق من أن فتح مشروع يطبع المسار بشكل صحيح على stdout بعد إغلاق واجهة Textual (القسم 6.6).
- اختبار يدوي شامل لكل معايير القبول في القسم 12 (عدا معيار عمل الأمر `projls` نفسه، لأنه يعتمد على المرحلة 2).

**مخرجات هذه المرحلة:** برنامج TUI يعمل ويُختبر محليًا داخل مجلد المشروع، بدون تثبيت.

### المرحلة 2 — التغليف (Packaging)

الهدف: تحويل الكود من مجرد سكربتات إلى برنامج جاهز للتثبيت والعمل مباشرة من الطرفية بأمر `projls`.

يشمل ذلك:
- تجهيز الحزمة للتثبيت عبر pip (`pyproject.toml`، نقطة الدخول `projectslister`).
- تنفيذ آلية التعديل التلقائي لملفات تهيئة الطرفية الموضّحة في القسم 8.2، بحيث يقوم البرنامج نفسه بإضافة دالة `projls` المناسبة (bash أو PowerShell) دون أي تدخل يدوي من المستخدم في الملفات.
- اختبار التثبيت الكامل من الصفر على Windows وLinux/Ubuntu: تثبيت الحزمة → تشغيل أمر تثبيت الدالة → فتح طرفية جديدة → تجربة `projls` فعليًا والتأكد من أنه ينقل المجلد بنجاح.
- التحقق من بقية معايير القبول في القسم 12 كاملة بعد التثبيت الفعلي.

**مخرجات هذه المرحلة:** حزمة قابلة للتثبيت (`pip install .` أو من PyPI لاحقًا)، وأمر `projls` يعمل مباشرة من أي طرفية بعد التثبيت دون تعديل يدوي من المستخدم.
