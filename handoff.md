# ProjectsLister (projls) - Handoff Document

## نظرة عامة
**ProjectsLister** هو برنامج طرفية تفاعلي (TUI) مُصمم لتسريع التنقل بين مجلدات المشاريع الكثيرة الاستخدام عبر سطر الأوامر.

## الميزات الأساسية
- **واجهة TUI:** تعتمد على مكتبة [Textual](https://textual.textualize.io/) لتوفير واجهة تفاعلية بالكامل عبر لوحة المفاتيح.
- **إدارة المشاريع:** إضافة، تعديل، وحذف المشاريع مع حفظ المسارات في ملف `projects.json` في مجلد إعدادات النظام القياسي.
- **الترتيب الذكي:** عرض المشاريع مرتبة حسب تاريخ آخر فتح (MRU).
- **البحث الحي:** فلترة المشاريع أثناء الكتابة.
- **التكامل مع الطرفية:** أمر `projls` الذي يغير مجلد الطرفية الحالية عند فتح مشروع.

## المكدس التقني
- **اللغة:** Python 3.10+
- **الواجهة:** Textual (لـ TUI) و Rich (لتنسيق النصوص).
- **التخزين:** ملف `projects.json` (مع تحديد المسار عبر `platformdirs`).
- **التوزيع:** حزمة Python قابلة للتثبيت عبر `pip`.

## مراحل البناء
1. **Core Build:** تطوير المنطق الأساسي للبرنامج، نموذج البيانات، والواجهة التفاعلية (بدون التثبيت).
2. **Packaging:** تجهيز هيكل المشروع للتثبيت (`pyproject.toml`)، وتطوير آلية التثبيت التلقائي (`installer.py`) لتكامل دالة `projls` مع ملفات تهيئة الطرفية (Bash/PowerShell).

## كيفية التثبيت والاستخدام
1. **التثبيت:** من داخل مجلد المشروع: `pip install .`
2. **التكامل:** تشغيل `projectslister --install-shell` لمرة واحدة لإضافة أمر `projls` تلقائيًا.
3. **الاستخدام:** كتابة `projls` في أي طرفية لفتح الواجهة والتنقل بين المشاريع.
